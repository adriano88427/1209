原表格分析处理模块进一步修改分析报告MD

## 1. 当前发现的主要问题
- 预处理导出命名易混淆：公共预处理与带参数预处理表名过于相似。
- 百分比列出现负值/大于 1：源表无此极端值，processed 表因缩尾 + 标准化（winsor + zscore）而出现负值和 >1；`__raw` 目前保存的是处理后的副本，并非真实原始值。
- 按年份缺列：上市日期、现价、经营范围等字段在部分年度文件缺失，合并后全年为空。

## 2. 原因分析
- 值域异常：标准化会让数据围绕均值波动，出现负值和 >1 属 zscore 正常表现；但与“原始比例”语义不符。`__raw` 未保留真原始值，导致核查看到的也是处理后结果。
- 年度缺列：2021 文件含全部四列；2022/2023/2024 仅有上市日期、户均持股比例；2025 四列都不存在，导致这些列在合并后大量缺失。
- 命名混淆：单/带参数预处理共用“预处理数据_核查”前缀，人工识别困难。

## 3. 已落实的调整（代码已修改）
- 年度缺失检测：预处理阶段发现因子在覆盖年份中有年份全缺，则跳过因子分析并记录 `missing_year_factors`；报告新增“缺失列按年度说明”板块（列出缺失年份和各年非空行数）。
- 预处理导出命名分离：公共预处理导出改为“预处理数据_单因子_核查_{timestamp}.xlsx”，带参数预处理导出为“预处理数据_带参数_核查_{timestamp}.xlsx”。

## 4. 待进一步改进（结合可行性/风险）
- 保留真原始值（高优先）：新增“未缩尾未标准化”导出，让 `__raw` 指向真实原始值；处理版单独存储/命名，兼顾核查与计算。
- 可选限幅/过滤（谨慎开启）：比例/持仓类可配置轻量 winsor/clip，默认关闭；如开启需在报告注明参数，并与关闭版对照。
- 缺列说明与告警：保持“缺失列按年度说明”，必要时在汇总 CSV 增加“缺列告警”列（仅告知，不强剔除）。
- 命名兼容性：命名调整可能影响旧脚本，必要时可保留过渡期旧名或在说明书中强调变更。

## 5. 后续行动建议
1) 实施“原始版+处理版”双表导出，调整 `__raw` 生成位置（处理前备份），处理版可继续缩尾+标准化供计算；处理版文件名中明确标注“已中性化”。  
2) 处理版仅导出一次（避免同一流程重复生成），保留最新（中性化后）一份。  
3) 对按年份全缺的因子保持跳过并在报告说明；如需更严/宽策略，可配置化。  
4) 限幅开关保持关闭，仅在报告中提示可选的 0.01%/99.9% 分位限幅方案（警告性质，不启用）。  
5) 如有自动脚本依赖旧命名，提供过渡期兼容或更新说明。 
