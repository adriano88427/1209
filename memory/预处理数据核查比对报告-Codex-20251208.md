预处理数据核查比对报告-Codex-20251208

## 对象
- 原始版：预处理数据_单因子_原始_20251208_125602.xlsx（仅清洗/别名/数值化/日期转换，未缩尾、未标准化、未中性化）。
- 处理版：预处理数据_单因子_核查_20251208_125620.xlsx（清洗 + 缩尾/标准化 + 中性化，用于实际计算）。
- 原始 Excel（2021-2025 合并）：用于范围对照。
- 列级对比摘要：`baogao/预处理原始_vs_处理_列对比摘要.csv`（逐列统计非空数、最小/最大/均值，原始 vs 处理）。
- 带参数输入版：`预处理数据_带参数输入版_用于带参数因子分析_20251208_134612.xlsx`（param_input 口径，未缩尾/未标准化/未中性化），逐列统计：`baogao/带参数输入版_逐列统计.csv`。

## 快速核对（核心列）
- 行数：原始版 21,041 行；处理版 21,036 行（少 5 行，为删除收益缺失行）。
- QFII持仓占比  
  - 原始版：min 0.0000，max 0.2030，mean 0.0019，std 0.0071（与源表一致，无负值）。  
  - 处理版：min -3.8853，max 5.7728，mean -0.00023，std 0.92（zscore 后中心化、接近单位方差，出现负值/大于1属标准化现象）。  
  - 源表（逐年）：max 0.203，未见负值。  
  结论：值域异常来自标准化，非源表数据错误。
- 当日回调  
  - 原始版：min -0.0001，max 0.3461，mean 0.0483（与源表 2021-2025 范围一致，无离谱值）。  
  - 处理版：min -3.3555，max 3.4835，mean 0.0068，std 0.86（zscore 后）。  
- 流通市值(元)  
  - 原始版：min 8.2e8，max 2.50e12，mean 8.57e9（与源表一致）。  
  - 处理版：min -0.59，max 6.32，mean 2.6e-05，std≈1.0（标准化后）。
- 当日最高涨幅__raw、次日开盘涨跌幅__raw：处理版保留原始比例列，范围与源表一致（约 [-0.20,0.20] 等），用于核查。

## 缺失列与跳过
- 按年度缺列说明：2021 有上市日期/户均持股/现价/经营范围；2022-2024 仅有上市日期、户均持股；2025 四列皆无。缺失列已在报告“缺失列按年度说明”中呈现，缺失年份的因子被跳过分析。

## 异常评估
- 处理版的负值/大于1为标准化结果，符合预期，不是数据损坏。  
- 原始版保留了真实比例/金额，范围与源表一致，便于人工核查。  
- 行数差异仅来自删除收益缺失行（5 行），符合清洗规则。
- 列级检查：生成了逐列摘要 CSV，确认各列处理后均值/范围与标准化逻辑一致，无异常超出标准化语义的值。
- 带参数输入版逐列统计：空列提示（源文件缺失或未匹配别名）包括 股票名称、信号日期、筹码集中度、所属同花顺行业、经营范围；其余列数值范围与原始口径一致，无异常 max<min 等问题。

## 结论
- 预处理流程（清洗/别名/数值化 → 缩尾+标准化 → 中性化）在实现与核查层面工作正常，处理版的值域异常均为标准化现象，未发现数据损坏或超出语义的异常。
- 原始版/带参数输入版与源表范围一致，便于核查和带参数分析参考；行数差异仅因剔除收益缺失行，符合规则。
- 可视为当前预处理流程符合常规计算标准、可用于后续因子分析；若需更稳健，可选进一步对照（如限幅开启/关闭、低唯一值因子业务评估）。
- 未发现处理后超出标准化语义的异常；唯一值低、区间跨度告警仍需在后续报告提示。

## 是否满足因子分析数据标准
- 原始版：满足“原始比例/金额”核查需求，可作为参数化/对照输入。  
- 处理版：标准化+中性化后，均值≈0、方差≈1，适合 IC/分组计算；值域异常仅为标准化表现，不影响计算正确性。

## 后续建议
1) 保留当前双表导出，处理版用于计算，原始版用于核查。  
2) 报告中提示限幅可选项（0.01%/99.9%），默认关闭。  
3) 如需减少文件数量，可将处理版导出次数限制为一次（当前已计划只保留中性化后的处理版）。  
4) 继续在报告中提示唯一值占比过低/区间跨度告警，便于业务侧评估信号质量。 
