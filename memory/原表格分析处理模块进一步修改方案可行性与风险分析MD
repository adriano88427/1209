原表格分析处理模块进一步修改方案可行性与风险分析MD

## 可行性
- 技术可行：导出多版本（原始/处理）和调整 `__raw` 逻辑均为增量改动，影响范围可控；报告新增字段/说明易于实现。
- 兼容性：不改变主计算流程（仍可用处理版数据）；原始版仅用于核查/对照，默认不干扰现有分析。
- 配置化：限幅开关、缺列告警列、命名分离等都可通过配置或函数参数控制，便于渐进启用。

## 风险点与缓解
1) `__raw` 语义调整：若实现不慎，可能与历史报告/脚本期望不一致。  
   - 缓解：明确版本变更，保留旧列或新增 `_processed`，确保下游引用不破坏；上线前对比一版输出。
2) 原始/处理双表输出：文件体积与运行时间小幅增加。  
   - 缓解：可配置开关，默认保留；必要时仅保留核查样本截取。
3) 限幅开关：若开启不当可能掩盖真实极值。  
   - 缓解：默认关闭，报告中注明限幅参数；对比开启/关闭差异再决定。
4) 缺列告警：自动跳过因子可能导致可用因子减少。  
   - 缓解：仅对“覆盖年份全缺”的因子跳过，并在报告/日志明确列出；必要时可允许强制保留。
5) 命名调整：历史自动化脚本若依赖旧文件名可能失效。  
   - 缓解：改名前告知，或同时输出旧名做过渡期；更新说明文档。

## 结论
- 方案可行，风险主要在兼容性与配置选择。建议：先上线“__raw 修正 + 原始版导出 + 命名分离”，保持限幅开关关闭，并做一版输出对比确认无回归；再视需求决定是否启用限幅或缺列告警列的扩展输出。
