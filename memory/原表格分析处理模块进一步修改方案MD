原表格分析处理模块进一步修改方案MD

## 1. 命名与导出
- 公共预处理与带参数预处理表明确区分命名：保留“预处理数据_单因子_核查_{ts}.xlsx”“预处理数据_带参数_核查_{ts}.xlsx”。
- 追加“原始未处理版”导出：仅做列别名匹配与数值化，不缩尾、不标准化，供人工核查和参数化入口对照。
- 处理版导出继续用于分析（缩尾+标准化），但报告中注明计算使用处理版。

## 2. 原始值保留与 __raw 语义
- 调整 __raw：在任何缩尾/标准化前先备份原始数值，确保 __raw 为真实原始值；处理中/中性化后的值单独存储（如 _processed）。
- 报告中显示：原始范围、处理后范围，并提示当前计算基于处理后数据。

## 3. 按年份缺列处理
- 保持现有逻辑：若某因子在覆盖年份中存在“全年空列”，则跳过该因子分析并记录 `missing_year_factors`。
- 报告中保留“缺失列按年度说明”，列出缺失年份及各年非空行数；如需，可在 CSV 汇总中增加“缺列告警”列。

## 4. 值域与异常
- 对比例/持仓类列：可配置轻量 winsor/clip（如 0.5%/99.5%）开关，默认关；若开启，报告注明限幅参数。
- 对唯一值占比过低、区间跨度异常的因子，在报告中保持告警，默认不强行剔除。

## 5. 行动步骤（待确认后实施）
1) 增加原始未处理导出，并调整 __raw 生成位置；导出“处理版/原始版”双表。
2) 在报告中标注计算口径（标准化版）和原始范围对照。
3) 可选：为比例类列添加可配置限幅开关（默认关闭）。
4) 按年份缺列说明保留，并可在汇总 CSV 增加“缺列告警”列。

（本方案待确认后再实施代码改动。）
